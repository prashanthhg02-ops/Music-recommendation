# Echo

A dependency-free music recommendation interface powered by a small AIML-style mood intent layer.

## Run

Open `index.html` in a browser. No build step or package install is required.

## AIML behavior

The recommendation rules live in `app.js`. Mood buttons map to recommendation sets, while the Ask Echo input detects lightweight intent keywords such as `night`, `party`, `road`, and `sun`.
